/*2- Exibir os números pares de 0 a 60, fazer uso do laço
de repetição for
Nome: Guilherme Henrique Saores Campos
*/

let pares: number[] = [];
let indice = 0;

for (let i = 0; i <= 60; i++) {
    if (i % 2 === 0) {
        console.log(i);
    }
}
